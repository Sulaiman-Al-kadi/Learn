// Exercise 06 — Number stats + multiplication table
// See README.md for the exact format. Two parts: a while loop, then a nested for loop.

// TODO: declare accumulators: count, sum, max, min, evens
//       Tip: start max very low and min very high so the first real number always "wins":
//       int max = int.MinValue, min = int.MaxValue;

// TODO: while (true) loop — read a line, parse it, if it's 0 break, otherwise update accumulators

// TODO: read the table size

// TODO: print "Numbers: {count}", then (only if count > 0) the Sum/Average/Max/Min/Evens lines

// TODO: print "Table:" then the nested for loop, using {row * col,4} for alignment
