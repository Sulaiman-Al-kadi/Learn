// Exercise 08 — List toolkit
// Read n, then n integers into a List<int>. No LINQ — loops and List<T> methods only. See README.md.

int n = int.Parse(Console.ReadLine() ?? "");
List<int> original = new List<int>();
// TODO: read n integers into `original`

// TODO: List<int> sorted = new List<int>(original); sorted.Sort();

// TODO: List<int> reversed = new List<int>(original); reversed.Reverse();

// TODO: build `unique` by walking `original` with foreach, Add-ing only values not already Contained

// TODO: find the second-largest DISTINCT value from `unique` in one pass (track largest + second)

// TODO: print the five lines — see README.md for exact format
